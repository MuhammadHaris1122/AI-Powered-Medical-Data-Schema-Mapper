# AI-Powered-Medical-Data-Schema-Mapper
This project automatically converts a source medical dataset into the schema of a target dataset without hardcoded column mapping.  The system combines Natural Language Processing (NLP), semantic similarity, fuzzy matching, and rule-based validation to identify the best column mappings between two medical datasets.
